# Backend
DEEPGRAM_API_KEY=
DEEPGRAM_MODEL=nova-3-general
DEEPGRAM_LANGUAGE=multi  # 'multi' para mejor soporte de code-switching, 'es' para español puro
PLAYBOOK_PATH=data/playbook.json
USE_LLM_FALLBACK=false

# Audio Processing
USE_FFMPEG=false  # Set to true for PCM16 conversion (more compatible but higher bandwidth)

# Storage Configuration
USE_GCS=false
GCS_BUCKET_NAME=salescoach-calls
LOCAL_STORAGE_PATH=calls

# System Configuration
SESSION_TIMEOUT=5400  # 90 minutes in seconds
MAX_CONNECTIONS=20

# Frontend
NEXT_PUBLIC_API_WS=ws://localhost:8000/ws/demo
